apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: open5gs

resources:
  - ue-churn-service.yaml
  - ue-churn-statefulset.yaml

configMapGenerator:
  - name: ue-churn-configmap
    files:
      - entrypoint.sh
